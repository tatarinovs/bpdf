[package]
name = "bpdf"
version = "0.5.2"
edition = "2024"
rust-version = "1.97"
description = "Second-generation loss-conscious PDF and image processing toolkit"
license = "MIT"
repository = "https://github.com/tatarinovs/bpdf"
keywords = ["pdf", "merge", "ocr", "image", "cli"]
categories = ["command-line-utilities", "multimedia"]

[dependencies]
anyhow = "1.0.100"
basic-toml = "0.1"
clap = { version = "4.6.4", default-features = false, features = ["derive", "std", "help", "usage", "error-context", "string", "suggestions"] }
glob = "0.3.3"
image = { version = "0.25.10", default-features = false, features = [
    "bmp",
    "gif",
    "jpeg",
    "png",
    "tiff",
    "webp",
] }
lopdf = { version = "0.44.0", default-features = false }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0.151"
tempfile = "3.23.0"
ttf-parser = { version = "0.25.1", default-features = false, features = ["std"] }
ureq = { version = "3.3.0", default-features = false, features = [
    "json",
    "rustls",
    "socks-proxy",
] }
zip = { version = "2.2", default-features = false, features = ["deflate"] }
miniz_oxide = "0.8"

[target.'cfg(windows)'.dependencies]
windows = { version = "0.62.2", features = [
    "Win32_Graphics_Imaging",
    "Win32_Storage_FileSystem",
    "Win32_System_Com",
    "Win32_System_Console",
    "Media_Ocr",
    "Graphics_Imaging",
    "Storage_Streams",
    "Globalization",
    "Foundation_Collections",
    "Data_Pdf",
    "Storage",
] }

[profile.release]
strip = true
lto = "fat"
opt-level = "z"
codegen-units = 1
panic = "abort"

[dev-dependencies]
lopdf = "0.44.0"
